#include "main.h"
/**
 * add(int, int)- addtwo integers together
 * @numa: integer a
 * @numb: integer b
 * Result: Return with the added integers
 */
int add(int numa, int numb)
{
	return (numa + numb);
}
