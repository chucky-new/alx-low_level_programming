#include "main.h"
/**
 * _isalpha(int c) - check code
 * @c: upper or lower case
 * Return: 1 if c is lowercase, otherwise 0
 */
int _isalpha(int c)
{
	if ((c >= 'A' && c <= 'Z')|| 
			((c >= 'a' && c <= 'z')
			 return (1);
		else
	   return (0);
}
