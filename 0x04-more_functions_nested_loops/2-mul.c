#include "main.h"
#include <stdio.h>
/**
 * mul - a fuction to multiply
 * @a: first number input
 * @b: second number input
 * Return: multiplication of the given two inputs
 */
int mul(int a, int b)
{
	return (a * b);
}
